一个游戏网站，游戏的核心规则如下：

# 实时方格领土战争游戏 — 设计规格 v1.1

## 一、游戏概述

本游戏是一款**实时方格领土战争**游戏，设计灵感来自 Generals.io。玩家从一座主城和少量初始领土出发，通过生产兵力、向相邻格推出兵力、占领土地、建立指挥所、发展多个主城，最终摧毁敌方全部主城获胜。

游戏刻意不设兵种、科技树和技能。全部深度来自四个核心机制的相互作用：**兵力调度、领土连通、断链孤军、主城攻防**。玩家的核心决策不是「能不能打下来」，而是「扩张之后自己的网络会不会被切断」，以及「该消灭敌兵，还是直接切断敌方的连接结构」。

目标单局时长 **3～5 分钟**。操作简单、反馈即时、规则易懂，但高手能通过路线规划、兵力计算、时机把握和网络切割拉开明显差距。胜负的核心可以概括为一句话：**让自己的兵力始终保持有效连接，同时让敌人的兵力失去有效连接。**

## 二、时间与地图

### 2.1 Tick

服务器以固定 Tick 运行，**1 Tick = 0.5 秒**。所有周期性逻辑（主城生产、领土生产、爆发期加成、孤军衰减）都以 Tick 为单位。

### 2.2 地图与格子

地图为规则方格，格子间仅**四方向连通**（上下左右），对角不算连通。每个格子有固定地形和动态状态：

| 状态项   | 取值                   |
| -------- | ---------------------- |
| 地形     | 普通地块 / 沼泽 / 山脉 |
| 所属     | 中立 / 玩家            |
| 兵力     | 非负整数，**无上限**   |
| 建筑     | 无 / 指挥所 / 主城     |
| 领土状态 | 正常 / 孤军            |

地形与所属、兵力、建筑相互独立：沼泽被占领后仍是沼泽，不会变成普通地块。

## 三、三种地形

三种地形是地图设计的三门语言：**普通地块决定资源，沼泽决定节点，山脉决定路线。**

**普通地块**是经济与发展的载体：可占领、可存兵、可生产兵力、可建指挥所、可升级主城。

**沼泽**是纯粹的**战略位置**：可占领、可存兵、可参与连通、可作为网络中间节点，但永远不生产兵力、不能建任何建筑。占沼泽的目的是保连通、控要道、铺路或阻断，而不是发展经济。

**山脉**是**地形限制**：完全不可占领、不可通过、不可存兵、不可建建筑、不参与连通。向山脉推兵操作直接无效。山脉通过改变地图拓扑制造狭窄通道和战略瓶颈，本身不会主动制造断链。

| 属性                   | 普通 | 沼泽 | 山脉 |
| ---------------------- | ---- | ---- | ---- |
| 可占领 / 存兵 / 可攻击 | ✓    | ✓    | ✗    |
| 自动生产兵力           | ✓    | ✗    | ✗    |
| 爆发期加成             | ✓    | ✗    | ✗    |
| 建指挥所 / 升级主城    | ✓    | ✗    | ✗    |
| 参与连通路径           | ✓    | ✓    | ✗    |
| 可进入孤军             | ✓    | ✓    | ✗    |

## 四、经济与生产

- **主城**：每 Tick **+1** 兵（每秒 +2）。可存兵，是连通网络的锚点。玩家可拥有多座主城，初始主城编号 1，后续按建立顺序递增。
- **普通地块**：每 **50 Tick +1** 兵（仅正常状态）。
- **指挥所**：每 **50 Tick +1** 兵（仅正常状态，不享受爆发期加成）。
- **沼泽 / 孤军 / 中立格**：永远不生产兵力。

### 4.1 爆发期

第 **26～50 Tick** 为全局爆发期：所有**正常状态的普通领土**每 Tick 额外 **+1** 兵（基础生产不受影响，主城、指挥所、沼泽、孤军、中立格均不享受）。爆发期同时作用于所有玩家。

爆发期的视觉提示必须是**地图最外侧的一圈红色边缘效果**（第 26 Tick 出现、随倒计时逐渐减弱、第 50 Tick 结束后消失）。红色不能贴在具体领土上，只表达「现在是全局爆发期」，避免被误读为局部危险。第 51 Tick 起游戏恢复正常经济，本版本没有任何后期额外机制（无随机高产、无疲劳、无缩圈等），后期战局由「兵力积累 → 网络布局 → 战略切断 → 孤军衰减 → 救援或放弃 → 主城攻防」自然解决。

## 五、操作与兵力移动

玩家选中己方正常领土格后，用 **WASD** 向四方向相邻格推出兵力。目标格须在地图内且非山脉，否则操作无效。目标是己方格 → 兵力转移（直接相加）；目标是中立或敌方格 → 攻击判定。

### 5.1 三种推兵模式

**普通 WASD —— 全兵。** 只保留 1 兵看守，其余全部推出：$\max(0,\;兵力-1)$。

**Z —— 半兵（一次性）。** 按智能分兵算出理论推出量后减半取整：$\lfloor 理论推出 / 2 \rfloor$。只影响下一次有效 WASD，执行后 Z 状态立即消失。理论推出为 1 时半兵为 0，不推兵。

**Shift + WASD —— 智能分兵。** 设当前格兵力为 A，除本次目标格外，其他三个方向上所有**非己方**相邻格可动兵力（对于每一个格子，指的是，兵力 - 1）总和为 B（己方孤军仍算己方，不计入），则：

$$保留 = B+1,\qquad 理论推出 = \max(0,\;A-B-1)$$

关键规则：**本次指向的目标格不参与保留计算**——主动选择攻击的方向不视为需要防守的方向。Shift 会忽略（不消耗）Z 状态。

**优先级：Shift > Z > 普通。** Shift 与 Z 同时生效时执行智能分兵。

### 5.2 攻击与占领

攻击成功的充要条件是**攻击兵力严格大于防守兵力**。攻击成功后目标格立即归属攻击方，防守兵力全灭，**该格新兵力 = 推出兵力 − 防守兵力**。**进攻失败（攻击兵力 ≤ 防守兵力，含相等）时双方结算损耗**：进攻方损失全部推出兵力，防守方兵力等量扣除（最低减到 0；归属与建筑不受影响，被减到 0 的格仍归原主）。

建筑是格子的属性而非独立单位：**占领建筑 = 摧毁原建筑并占领该格**。敌方指挥所/主城被占领后，该格成为攻击方的普通领土，不继承建筑。若被占领的是敌方最后一座主城，立即触发胜利。

### 5.3 建造与升级

- **X 建指挥所**：当前格为己方、正常状态、普通地块、兵力 ≥ 50，且**执行 X 之前**已连通己方任意主城或指挥所。成功后消耗 50 兵。指挥所每 50 Tick +1 兵（同普通地块基础经济），是连通锚点。
- **C 升级主城**：己方、正常状态的指挥所、兵力 ≥ 50，消耗 50 兵，指挥所变主城，立即获得每 Tick +1 生产与锚点资格。在普通地块上按 C 等效于连续两步操作：先建指挥所、下一 Tick 再升级主城（角标同时显示指挥所与皇冠两个图标）。
- **队列建造**：X/C 与推兵共用同一操作队列（每 Tick 每玩家执行一条）。按键入队后目标格右下角显示小型指挥所/皇冠角标；轮到该操作的下一 Tick 瞬间完成建造，角标切换为完整建筑。入队只校验地形（普通地块），归属/兵力/连通在执行时校验；**条件不满足的建造操作到达队首时自动跳过**，队列继续执行后续操作，不产生任何消耗。
- 建筑只能存在于普通地块；沼泽与山脉禁止一切建筑。

### 5.4 空格导航

空格将光标跳转到曼哈顿距离最近的己方主城；距离相同时取编号较小者。不产生任何状态变化。**镜头仅在目标主城不在当前视野内时才拖动居中**；已在视野内时镜头保持不动。

### 5.5 无效操作

未选中己方正常领土、选中孤军、目标出界或为山脉、推兵兵力不足（推出量为 0）时操作无效，且不得产生任何隐形资源消耗。X/C 入队时不校验归属与兵力（允许排在队列未来位置），执行时条件不满足则自动跳过、不消耗资源。**光标不受操作有效性影响**：WASD 后光标总是移动到相邻目标格（出界/山脉除外）；无效推兵不入队、不显示箭头，但光标保持可移动。

## 六、领土连通与孤军

### 6.1 连通判定

己方格子能通过连续的己方格（普通地块或己方沼泽，四方向）到达己方**任意主城或指挥所**时为正常领土。连通路径不能经过山脉、敌方领土、中立格。

任何可能改变连接关系的事件（任何占领、建筑被毁、新建指挥所、升级主城等）发生后，系统必须重算受影响区域。可用局部图搜索优化，但结果必须等价于完整四方向连通判定。

### 6.2 断链与孤军

一片己方领土失去与所有主城/指挥所的连接时立即断链，进入**孤军状态**。孤军不是中立，仍属于原玩家。

- **断链瞬间兵力减半**：$\lfloor 兵力/2 \rfloor$，只在进入孤军瞬间执行一次；特例：1 兵减半仍为 1，不因减半归零。
- **孤军失去一切主动性**：不可选中、不可推兵、不可建造、不生产、不吃爆发期加成。
- **断链宽限期**：断链后的前 5 回合（10 Tick）为宽限期，只做快速闪烁提示，不真正衰减。
- **持续衰减**：第 6 回合起（断链后第 11 Tick 开始），每回合（每 2 Tick）衰减当前兵力的 5%：

$$decay = \max(1,\ \lceil current \times 0.05 \rceil)$$

取整后从当前兵力中扣除，基于当前兵力动态计算，呈减速的指数衰减；因最低减 1，最终必归零。兵力归零的格立即变中立（归属、建筑全部清除）。

- **可被攻击**：规则与普通领土完全一致（严格大于），无额外防御。
- **可重新连接**：孤军的某个**连通组件**重新接上己方主城/指挥所时，该组件所有格立即恢复正常，每格兵力按**当前兵力 ×2**（不是恢复断链前的值），恢复经济与操作资格。
- **以当前连通组件为单位**：孤军区域不是固定对象。敌人切断孤军中间节点会使其分裂，各组件独立判断恢复、独立衰减，不共享状态。

孤军的视觉为**闪烁动画**（表达「已断联」）：宽限期内快速闪烁，进入衰减期后转为常速闪烁；不随兵力减少改变颜色；恢复正常或归零时消失。

## 七、胜负

**最后主城决胜制**：玩家至少拥有一座主城即存活；最后一座主城被占领时立即出局，游戏结束。败者剩余领土不转移给胜者，全部保持原归属并直接进入孤军状态（不执行断链减半，指挥所/主城锚点全部拆除），随后按孤军规则自然衰减至中立，呈现「核心被毁、势力失守」的结局画面。

## 八、架构与实现约束

### 8.1 服务器权威

服务器是游戏状态的唯一权威，负责兵力、归属、建筑、战斗、连通、孤军、衰减、胜负的全部判定。客户端只负责输入、镜头、动画和视觉表现（兵力数字、爆发期红边、孤军闪烁等），不得自行决定战斗结果。

### 8.2 Tick 处理顺序

每个 Tick 按以下固定顺序执行（实现可优化，但结果必须等价）：

1. **自动生产**：主城 +1；正常普通领土与指挥所基础经济；爆发期内正常普通领土额外 +1。
2. **玩家输入**：按 Shift > Z > 普通优先级处理 WASD/Z/X/C。
3. **结算**：友军转移、攻击判定、建筑摧毁、归属变更。
4. **重算连通**：判定哪些区域断链。
5. **新断链处理**：立即减半并进入孤军（每格只执行一次；1 兵减半仍为 1）。
6. **孤军衰减**：已过 5 回合（10 Tick）宽限期的孤军格，每回合（每 2 Tick）执行一次 5% 衰减；宽限期内的格不衰减。
7. **归零与重连**：兵力 0 的孤军格变中立；重连的组件兵力 ×2 恢复正常。
8. **胜负判定**：任一玩家失去最后一座主城即结束游戏。

### 8.3 关键边界条件

- 兵力恰好 50 可建指挥所/升级主城，49 不行。
- 攻击 = 防守 → 进攻失败，防守方减至 0（保留归属与建筑）；攻击 = 防守 + 1 → 成功占领。
- 当前 1 兵时全冲推出 0；理论推出 1 时半兵推出 0。
- 向山脉推兵完全无效，无任何状态变化。
- 断链减半只执行一次，不得因重算连通而重复；1 兵断链减半仍为 1，不因减半归零。
- 孤军重连按当前兵力 ×2，不恢复原始兵力；分裂组件独立处理。

## 九、认知与体验目标

视觉反馈应让玩家**不读文字就理解局势**：颜色即归属；地图外圈变红 = 全局爆发期；领土持续闪烁 = 断链；孤军兵力流失 = 正在自行消亡；关键节点被打掉 = 后方可能全部变孤军。

期望的学习曲线：先会「WASD 推兵占地」→ 再会三种推兵模式的取舍 → 理解「扩张越多网络越脆弱」→ 理解「可以不消灭敌兵，只切断连接」→ 最终围绕兵力集中、路线选择、节点控制、断链与救援时机、假动作和心理博弈形成高水平策略。**深度来自简单规则涌现的复杂局面，而不是更多系统。**

---

游戏的基础上，支持用户系统，FFA，feed系统。准备一套统一的Rating系统（无需区分1v1和ffa）。feed系统在首页，称为动态，仿佛推特。允许评论和点赞。游戏房间内部支持选择观战。非观战的用户中，「我准备好了」用户超过向上取整的1/2则会自动开始。实时聊天框跨房间同步。
